
# apiVersion: autoscaling/v1
# kind: HorizontalPodAutoscaler
# metadata:
#   name: dj-app-hpa
# spec:
#   maxReplicas: 0 # define max replica count
#   minReplicas: 0  # define min replica count
#   scaleTargetRef:
#     apiVersion: apps/v1
#     kind: Deployment
#     name: dj-app-deployment
#   targetCPUUtilizationPercentage: 70 # target CPU utilization
# ---
# apiVersion: autoscaling/v1
# kind: HorizontalPodAutoscaler
# metadata:
#   name: fl-app-hpa
# spec:
#   maxReplicas: 0 # define max replica count
#   minReplicas: 0  # define min replica count
#   scaleTargetRef:
#     apiVersion: apps/v1
#     kind: Deployment
#     name: fl-app-deployment
#   targetCPUUtilizationPercentage: 70 # target CPU utilization
  